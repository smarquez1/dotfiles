#!/bin/sh

# Modules
# -------

# Sets sane Zsh built-in environment options.
zmodule environment
# Provides handy git aliases and functions.
zmodule git
# Applies correct bindkeys for input events.
zmodule input
# Sets a custom terminal title.
zmodule termtitle
# Utility aliases and functions. Adds colour to ls, grep and less.
zmodule utility

# Prompt
# Exposes git repository status information to prompts.
zmodule git-info
# Theme
# zmodule gitster
# zmodule sindresorhus/pure --source async.zsh --source pure.zsh
# Additional completion definitions.
zmodule zsh-users/zsh-completions
# Smart and extensive tab completion. must be sourced after zsh-users/zsh-completions
zmodule completion
# autosuggestions for Zsh.
zmodule zsh-users/zsh-autosuggestions
# syntax highlighting. must be sourced after completion
zmodule zsh-users/zsh-syntax-highlighting
# history search (up arrow).  must be sourced after zsh-users/zsh-syntax-highlighting
zmodule zsh-users/zsh-history-substring-search
# Ruby
zmodule zimfw/ruby

# Oh My Zsh plugins
# Extract compressed files
# zmodule ohmyzsh/ohmyzsh -f 'plugins/extract' -s 'plugins/extract/extract.plugin.zsh'
