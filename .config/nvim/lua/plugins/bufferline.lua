local map = require('utils').map

require('bufferline').setup({
		options = {
			numbers = "ordinal"
		}
	})

-- map("gb", [[<cmd>BufferLinePick<CR>]])
-- map("<leader><tab>", [[<cmd>BufferLineCycleNext<CR>]])
-- map("<S-tab>", [[<cmd>BufferLineCyclePrev<CR>]])
-- map("[b", [[<cmd>BufferLineMoveNext<CR>]])
-- map("]b", [[<cmd>BufferLineMovePrev<CR>]])
