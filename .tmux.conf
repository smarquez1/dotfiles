#  Tmux configuration
#####################

set-option -g default-terminal "tmux-256color"
# set-option -g default-terminal "screen-256color"
# start window index at 1
set -g base-index 1
# start session index at 1
setw -g pane-base-index 1
# re-number windows when one is closed
set -g renumber-windows on
# Enable mouse
set -g mouse on
# Interact with xterm clipboard
set -g set-clipboard on
# fix the escape delay problem when using vim
set -sg escape-time 0
# Fix for Neovim cursor shape
set -g -a terminal-overrides ',*:Ss=\E[%p1%d q:Se=\E[2 q'
# Enable visual bell
set -g visual-activity on
set-window-option -g visual-bell off
# Automatically set window title
set-option -g status-interval 1
# Don't close terminal when destroying session
set-option -g detach-on-destroy off

###########################
#  Key Bindings
###########################

# Set tmux to Vi mode
set -g @shell_mode 'vi'

# vim copy selection
unbind-key [
bind-key ESCAPE copy-mode

# remap prefix
unbind C-b
set -g prefix C-space
bind C-space send-prefix

unbind r
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# Create new session
bind C new-session

# last active window
bind-key C-space last-window

# splits
bind s split-window -v -c '#{pane_current_path}'
bind v split-window -h -c '#{pane_current_path}'

# Smart pane switching with awareness of vim splits
is_vim='echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?)(diff)?$"'
bind -n C-h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
bind -n C-j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
bind -n C-k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
bind -n C-l if-shell "$is_vim" "send-keys C-l" "select-pane -R"
bind -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"

###########################
# Appearance
###########################

# status
set -g status-bg "colour237"
set -g status-left "#{prefix_highlight}#[bg=blue]#[fg=colour235,bold] 🗁  #S "
set -g window-status-format '#[fg=cyan] #I #W '
set -g window-status-current-format '#[bg=cyan]#[fg=colour235,bold] #I #W '
set -g status-right "#[fg=cyan, bold]#{battery_icon}#{battery_percentage} #[bg=cyan]#[fg=colour235] 🕑 %H:%M "
set -g status-justify left
set-option -g status-position bottom

# messaging
set -g message-attr bold
set -g message-fg "colour00"
set -g message-bg cyan

##########################
# Plugins
##########################

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-pain-control'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
# set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-battery'

set -g @prefix_highlight_fg 'colour235'
set -g @prefix_highlight_bg 'yellow'
set -g @prefix_highlight_show_copy_mode 'on'
set -g @prefix_highlight_copy_mode_attr 'fg=black,bg=magenta'
#set -g @resurrect-strategy-nvim 'session'
#set -g @resurrect-capture-pane-contents 'on'
# set -g @continuum-restore 'on'

run '~/.tmux/plugins/tpm/tpm'
